---
# vars file for glusterfs
glusterfs_packages:
  default:
    - glusterfs
  Debian:
    - glusterfs-server
  Ubuntu:
    - glusterfs-server

glusterfs_selinux_local_policies:
  - my-glusterfsd
  - my-glusterepoll0
