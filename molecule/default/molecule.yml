---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: glusterfs-alpine-edge
    image: alpine:edge
    command: sh -c "while true ; do sleep 1 ; done"
  - name: glusterfs-alpine-latest
    image: alpine:latest
    command: sh -c "while true ; do sleep 1 ; done"
  - name: glusterfs-archlinux
    image: archlinux/base
  - name: glusterfs-centos-6
    image: centos:6
  - name: glusterfs-centos-latest
    image: centos:latest
  - name: glusterfs-debian-latest
    image: debian:latest
  - name: glusterfs-debian-stable
    image: debian:stable
  - name: glusterfs-debian-unstable
    image: debian:unstable
  - name: glusterfs-fedora-latest
    image: fedora:latest
  - name: glusterfs-fedora-rawhide
    image: fedora:rawhide
  - name: glusterfs-opensuse-leap
    image: opensuse:leap
  - name: glusterfs-opensuse-tumbleweed
    image: opensuse:tumbleweed
  - name: glusterfs-ubuntu-artful
    image: ubuntu:artful
  - name: glusterfs-ubuntu-devel
    image: ubuntu:devel
  - name: glusterfs-ubuntu-latest
    image: ubuntu:latest
provisioner:
  name: ansible
  inventory:
    host_vars:
      glusterfs-fedora-rawhide:
        ansible_python_interpreter: /usr/bin/python3
    group_vars:
      all:
        glusterfs_ignore_docker: no
  lint:
    name: ansible-lint
  config_options:
    defaults:
      callback_plugins: /usr/lib/python2.7/site-packages/ara/plugins/callbacks
scenario:
  name: default
verifier:
  name: goss
  lint:
    name: yamllint
