---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: glusterfs-centos-latest
    image: centos:latest
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        glusterfs_ignore_docker: no
  lint:
    name: ansible-lint
scenario:
  name: centos-latest
